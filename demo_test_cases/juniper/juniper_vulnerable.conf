# ==============================================================================
# JUNIPER JUNOS — VULNERABLE CONFIGURATION (WITH COMPLIANCE VIOLATIONS)
# Contains: Cleartext Telnet/HTTP, plaintext credentials, missing idle-timeout
# ==============================================================================
system {
    host-name SRX-SEC-VULN;
    login {
        user admin {
            uid 2002;
            class super-user;
            authentication {
                plain-text-password "admin12345";
            }
        }
    }
    services {
        telnet;
        web-management {
            http;
        }
    }
}
snmp {
    community public {
        authorization read-only;
    }
}
